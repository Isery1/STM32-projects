# RFID_Pi — ignore secrets, bytecode, and local venvs (this folder is the Pi client package).

# Secrets & local config (use .env.example as a template)
.env
.terminal_api_key

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
*.so

# Runtime / debug
app_debug.log
*.log
